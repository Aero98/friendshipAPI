declare type DateValue = string | number | Date;
interface TravelOptions {
	/** relative point of reference to travel */
	from?: DateValue;
	/** relative days to travel in number */
	to: number;
}
export declare const dt: {
	readonly now: Date;
	new: (d?: DateValue | undefined) => Date;
	format(date: DateValue, mask?: string, base?: 'UTC' | undefined): string;
	travel({ from, to }: TravelOptions): Date;
};
export {};
