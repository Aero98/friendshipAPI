export { dt } from './temporal';
export { random } from './toolbox';
interface CapitalizeOptions {
	/** only capitalize the very first letter */
	cap?: boolean;
	/** convert the remaining word to lowercase */
	normalize?: boolean;
}
export declare function capitalize(text: string, { cap, normalize }?: CapitalizeOptions): string;
export declare const tryNumber: <T>(text: T) => number | T;
