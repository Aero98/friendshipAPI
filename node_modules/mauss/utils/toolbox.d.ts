export declare const random: {
	float(max?: number, min?: number): number;
	int(max?: number, min?: number): number;
	bool(): boolean;
	array(length: number, max: number, min?: number): Array<number>;
	key<T>(dict: Record<string, T>): string;
	val<T_1>(dict: Record<string, T_1>): T_1;
	hex(): string;
	ipv4(): string;
	/** unsafe and for fast prototyping only, use with caution for production */
	uuid(): string;
};
